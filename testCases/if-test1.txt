main
var x, y, z;
array [ 4 ] a;
array [ 4 ][ 4 ][12] b;
var c;

procedure bar( x, z );
var i, j, e;
{
	let b[i] <- (j + (0 * 2 )) / 3;
	let j <- 0 +1 +(9 + 3 * c) /9 * b * a +11;
};

{
	let x <- 0;
	let y <- 0;
	let z <- 0;
	if y < x+10 then
		let z <- x+y;
		let x <- 10
	else
		let x <- 0;
		if y > 10 then
			let z <- x
		else
			let z <- 1
		fi;
		let y <- 10;
		let z <- 5
	fi;
	
	call OutputNum(c);
	
}.