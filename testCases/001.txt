main
var x, y, z;
array [ 4 ] a;
array [ 4 ][ 4 ] b;
var c;

procedure bar( x, z );
var i, j, e;
{
	let i[b] <- j + (4 + 2 );
	let j <- 0;
};

{
	let x <- 0;
	let y <- 0;
	let z <- 0;
	
	let c <- foo;
	
}.