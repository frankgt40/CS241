#Simple parsing test
main

var rsl;
function add(a, b);
var g, f;
{
	let g <- 1;
	let f <- 1;
	
	let g <- g + f + a + b;

	return g;
};

function sub(a, b);
{
	let a <- a - b;
	return a;
};

{
	call OutputNum(22222);
	let rsl <- call add(1, 2);
	call OutputNum(rsl);
	call OutputNewLine();
	call OutputNum(22222);
}
.