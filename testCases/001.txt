#Simple parsing test
main
var input;

function add(a, b);
var i, f;
{
	let i <- 1;
	let f <- 1;
	
	let i <- i + f + a + b;
	return i;
};

function sub(a, b);
{
	let a <- a - b;
	return a;
};

{
	let input <- call InputNum();
	let input <- 9 / input;
	let input <- 5 - input;
	let input <- call add(1, 2);
	call OutputNum(input);
	let input <- call sub(4,2);
	call OutputNum(input);
	call OutputNewLine();
	call OutputNewLine();
	call OutputNum(5);
	call OutputNewLine();
	call OutputNum(7);
	call OutputNewLine();
	call OutputNum(9);
	call OutputNum(input);
}
.